# on definit toutes les combinaisons qui existent dans le fichier historique

func_table_comb_var <- function(myData0){

    comb_var <- data.frame(FACTOR=unique(myData0$FACTOR))
    
return(comb_var)
}
